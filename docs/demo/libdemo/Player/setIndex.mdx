---
sidebar_position: 3
---
# setIndex

```js
static setIndex = pos
```

`Player.setIndex` gets an integer and sets the audio index if the integer is in the list range.

To apply changes you should force reload the player.

Example:
```js
Player.setIndex(0).reload();
```