---
sidebar_position: 3
---
# getIndex

```js
static getIndex = ()
```

`Player.getIndex` returns the current audio index. It may be used with `getList` to see more info about current audio.

Example:
```js
var audio_info = Player.getList()[Player.getIndex()];
```