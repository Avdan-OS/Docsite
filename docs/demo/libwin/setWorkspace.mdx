---
sidebar_position: 2
---
# setWorkspace

```js
setWorkspace = ws_num
```

`setWorkspace` creates or switches to workspace by `ws_num` number. If workspace is empty, it will be erased, otherwise it will switch to an existing one.

Example:
```js
setWorkspace(1);
```