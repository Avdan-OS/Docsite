---
sidebar_position: 3
---
# About Scrollbar

`Scrollbar` class is used to control the third dock element with widgets.

It has 2 methods: [get](get) and [set](set).

`set` is used to set a widget in the scrollbar and `get` is used to get current scrollbar list.