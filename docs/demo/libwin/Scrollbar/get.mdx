---
sidebar_position: 3
---
# get

```js
static get = ()
```

`Scrollbar.get` returns an arrays of scroll widgets.

Example:
```js
var scroll_list = Scrollbar.get();
var current_widget = scroll_list[scroll_list.pos];
```