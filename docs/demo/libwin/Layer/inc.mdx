---
sidebar_position: 3
---
# inc

```js
static inc = ()
```

`Layer.inc` is used to increment `z-index` value. It should be used in the end of window impact function.

Example:
```js
Layer.inc();
```