---
sidebar_position: 3
---
# inc

```js
static inc = ()
```

`Layer.inc` is used to increment the `z-index` value. It should be used at the end of the window impact function.

Example:
```js
Layer.inc();
```