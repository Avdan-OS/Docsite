---
sidebar_position: 3
---
# About Layer

`Layer` class is used to control `z-index` value to set window visibility.

`inc` is used to increment current value and `get` is used to get it for your purposes.

Don't forget to increment layer value after you finish.