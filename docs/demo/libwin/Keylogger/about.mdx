---
sidebar_position: 3
---
# About Keylogger

`Keylogger` class is used to check, which keys are held or to add some general keybindings.

It also has `Keylogger.showKeys` variable. Set it `true` via console to see events info.