---
sidebar_position: 3
---
# About Keylogger

`Keylogger` class is used to check, which keys are holded.

It has 6 methods, but most used should be only [getKeys](getKeys), [getList](getList) and [press](press).

It also has `Keylogger.showKeys` variable. Set it `true` via console to event info.