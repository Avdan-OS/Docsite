---
sidebar_position: 3
---
# set

```js
static set = ws_num
```

`Workspace.set` creates or switches to the workspace by `ws_num` number. If the workspace is empty, it will be erased, otherwise, it will switch to an existing one.

Example:
```js
Workspace.set(2).add(anyWindow);
```