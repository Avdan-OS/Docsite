---
sidebar_position: 3
---
# close

```js
static close = (e, win)
```

`Window.close` is used to close a window. If Object is not a window, it will ireturn 1 and output an error.

Example:
```js
Window.close(e, win); // e - event
```