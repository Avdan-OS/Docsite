---
sidebar_position: 3
---
# About Window

`Window` is used to control window spawning. It controls the main id to unite elements with windows and wraps content in windows.

`get` is used to get the current id and `inc` is used as increment id, `make` creates a window Object and `close` is used to close a window.